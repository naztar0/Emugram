<local:HostedPage x:Class="Telegram.Views.Settings.SettingsEmulationPage"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:controls="using:Telegram.Controls"
                  xmlns:local="using:Telegram.Views"
                  xmlns:viewModels="using:Telegram.ViewModels.Settings" 
                  xmlns:services="using:Telegram.Services"
                  mc:Ignorable="d"
                  NavigationMode="Root">

    <Grid Background="{ThemeResource SettingsPageBackground}">
        <controls:TableListView x:Name="ScrollingHost"
                                ItemsSource="{x:Bind ViewModel.Presets}"
                                ChoosingItemContainer="OnChoosingItemContainer"
                                ContainerContentChanging="OnContainerContentChanging"
                                ItemClick="List_ItemClick"
                                IsItemClickEnabled="True"
                                SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=OneWay}">
            <ListView.Header>
                <controls:SettingsPanel IsHeader="True"
                                        Margin="0,0,0,-32">
                    <controls:HeaderedControl>
                        <controls:BadgeButton Content="{CustomResource AddEmulationPreset}"
                                          Style="{StaticResource GlyphBadgeButtonStyle}"
                                          Click="{x:Bind ViewModel.OpenCreateEmulationPreset}"
                                          Glyph="&#xE710;" />

                        <CheckBox IsChecked="{x:Bind services:SettingsService.Current.Emulation.PreventFullscreen, Mode=TwoWay}"
                                  Content="{CustomResource PreventFullscreen}"
                                  Style="{StaticResource SettingsCheckBoxStyle}" />
                    </controls:HeaderedControl>
                    <TextBlock Text="{CustomResource EmulationPresets}"
                               Style="{StaticResource BaseTextBlockStyle}"
                               Margin="24,16,24,36" />
                </controls:SettingsPanel>
            </ListView.Header>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderContainerStyle>
                        <Style TargetType="ListViewHeaderItem">
                            <Setter Property="Margin"
                                    Value="0" />
                            <Setter Property="Padding"
                                    Value="0" />
                            <Setter Property="MinHeight"
                                    Value="0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewHeaderItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.HeaderContainerStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Height="32" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:EmulationPresetViewModel">
                    <Grid Padding="12,6"
                          Background="{ThemeResource SettingsItemBackground}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind Title}"
                                   VerticalAlignment="Bottom"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{x:Bind UserAgent}"
                                   Style="{StaticResource InfoBodyTextBlockStyle}"
                                   VerticalAlignment="Top"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   Grid.Row="1" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Footer>
                <Border Height="60" />
            </ListView.Footer>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="False"
                                     Margin="24,0" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </controls:TableListView>
    </Grid>
</local:HostedPage>
