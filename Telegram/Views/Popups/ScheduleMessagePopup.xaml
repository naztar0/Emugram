<controls:ContentPopup x:Class="Telegram.Views.Popups.ScheduleMessagePopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:controls="using:Telegram.Controls"
                       mc:Ignorable="d"
                       PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
                       CloseButtonClick="ContentDialog_SecondaryButtonClick"
                       ButtonsLayout="Vertical"
                       IsDismissButtonVisible="True">

    <StackPanel>
        <CalendarDatePicker x:Name="Date"
                            Header="{CustomResource ScheduleMessageDate}"
                            DateChanged="Date_DateChanged"
                            Margin="0,0,0,12"
                            HorizontalAlignment="Stretch" />
        <TimePicker x:Name="Time"
                    Header="{CustomResource ScheduleMessageTime}"
                    TimeChanged="Time_TimeChanged"
                    BorderBrush="{ThemeResource ButtonBorderBrush}"
                    HorizontalAlignment="Stretch" />
    </StackPanel>
</controls:ContentPopup>
